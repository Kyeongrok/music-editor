<Window x:Class="WpfMusicEditor.Forms.UI.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="설정"
        Width="560" Height="280"
        Background="#1E1E1E"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2D2D30" />
            <Setter Property="Foreground" Value="#EEEEEE" />
            <Setter Property="BorderBrush" Value="#3F3F46" />
            <Setter Property="Padding" Value="4,3" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3F3F46" />
            <Setter Property="Foreground" Value="#EEEEEE" />
            <Setter Property="BorderBrush" Value="#55555B" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="Header" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource Header}" Text="모델 생성 위치" />
            <TextBlock Foreground="#999999" TextWrapping="Wrap"
                       Text="학습하거나 .pth에서 변환해 만든 모델(.onnx)을 저장할 폴더입니다. 비워 두면 기본 위치를 씁니다." />
            <Grid Margin="0,8,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding ModelsDirectory, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="1" Margin="6,0,0,0" Content="찾기…" Command="{Binding BrowseModelsDirCommand}" />
                <Button Grid.Column="2" Margin="6,0,0,0" Content="기본값" Command="{Binding UseDefaultModelsDirCommand}" />
            </Grid>
            <TextBlock Margin="0,8,0,0" Foreground="#4EC9B0" TextWrapping="Wrap" Text="{Binding EffectiveModelsDirText}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="닫기" Click="OnClose" />
        </StackPanel>
    </Grid>
</Window>
